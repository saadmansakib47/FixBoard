{
  "scripts": {
    "start": "npm run server && npm run client",
    "server": "cd server && nodemon server.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
